import React from "react";
import Grid from "./components/Grid";
import Layout from "./layout/Layout";

const App: React.FC = () => {
  return (
    <Layout>
        <Grid />
    </Layout>
  );
};

export default App;
